#include <stdio.h>

int main(void) {
    float fa, fb;
    printf("inout x and y: ");
    scanf("%f%f", &fa, &fb);
    printf("float 类型数据的打印结果为: ");
    printf("fa=%d\tfb=%d\n", fa, fb);
    return 0;
}
